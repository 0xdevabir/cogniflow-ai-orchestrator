export default function DashboardPage() {
  return (
    <main style={{ maxWidth: 720, margin: "6rem auto", padding: "0 1.5rem" }}>
      <h1>📊 Eval Dashboard</h1>
      <p style={{ color: "#666" }}>
        Phase 7+ — faithfulness scores, hallucination flags, cost / latency / carbon charts, model mix.
      </p>
      <p>
        <a href="/">← Back home</a>
      </p>
    </main>
  );
}
